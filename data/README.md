# Data

`derived/` contains compact numerical outputs generated by the analysis
scripts. `input/pre_ramadan_tbr.csv` contains only virtual-patient model
identifiers and simulated pre-Ramadan TBR values needed by the scenario
analysis.

Large raw trajectories are not versioned because they total roughly 2 GB.
They can be regenerated with the commands in the repository README.
